# fly.toml — Amazon KING BOT Owner Mode v1
app = 'job-alert-dnzwuw'
primary_region = 'lhr'

[build]
  dockerfile = 'Dockerfile'

[processes]
  app = 'python main.py'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1

[mounts]
  source = 'bot_data'
  destination = '/data'
